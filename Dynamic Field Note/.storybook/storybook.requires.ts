/**
 * Storybook Auto-Generated Requires
 * このファイルは自動生成されます
 */

import { getStorybookUI, configure } from '@storybook/react-native';

// Storiesの自動読み込み
configure(() => {
  // ここにstoriesが自動的に追加されます
  // 手動で追加する場合は以下のように記述:
  // require('../src/components/Button/Button.stories');
}, module);

export const view = { getStorybookUI };
